//Objetos-métodos ⇒ Um objeto pode ter funções

/*
var calcMedia = () => {
    return (this.nota[0] + this.nota[1]) / 2; // this = isso, o this passa a ser o objeto (aluno), função está atribuida a um objeto por isso está funcionando o this
};

var aluno = new Object();{

     aluno.nome = "Gustavo",
     aluno.notas =  [10, 9],
     aluno.media = calcMedia //chamando a função
};

console.log(aluno.nome);
console.log(aluno.media()) //printando as notas de acordo com a função

*/
